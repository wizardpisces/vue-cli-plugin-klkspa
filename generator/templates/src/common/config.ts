const locales = {
    'en-US': {
        iso: 'en-US',
    },
    'zh-CN': {
        iso: 'zh-CN',
    },
    'zh-TW': {
        iso: 'zh-TW',
    },
};

const localesList = Object.keys(locales);

const defaultLocale = 'en-US';

export {
    locales,
    localesList,
    defaultLocale,
};

